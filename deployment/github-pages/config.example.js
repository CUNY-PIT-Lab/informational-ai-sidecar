/**
 * Public GitHub Pages runtime configuration.
 *
 * Copy this file to `config.js` during the future Pages build. The API base URL
 * is public by design. Never add OLLAMA_API_KEY or another provider credential.
 */
window.FORTUNE_GUIDE_CONFIG = Object.freeze({
  apiBaseUrl: "REPLACE_WITH_HTTPS_BACKEND_BASE_URL"
});
